# 018 Shadcn Primitive UI Rule

## Context

- User requested that checkboxes, dropdowns, text fields, dialogs, and similar controls always use themed shadcn-based primitives and be reused rather than styled ad hoc.

## Work Log

- Updated `AGENTS.md` to require shared, themed primitives for UI fundamentals.
- Updated `DESIGN.md` implementation rules with the same requirement and noted that existing ad hoc/default controls should be migrated when touched.

## Verification

- Documentation-only change; no build run.
