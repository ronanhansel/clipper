# Normal Form Tracking

- Updated shared `Input` and `Select` primitives to include `normal-case tracking-normal`.
- This prevents inspector fields nested inside uppercase/tracked labels from inheriting wide letter spacing.
- Select dropdown items also reset to normal tracking so open menus match the trigger typography.
