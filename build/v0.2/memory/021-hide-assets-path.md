# Hide Assets Path

- Removed the visible `assetsPath` line from the file manager panel.
- Kept asset copy-path behavior unchanged; `copyAssetPath` still resolves paths from project data in the parent component.
