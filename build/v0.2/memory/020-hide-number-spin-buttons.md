# Hide Number Spin Buttons

- Added global base CSS for `input[type="number"]` to hide native browser up/down spin buttons app-wide.
- Uses WebKit pseudo-element resets for Electron/Chromium and `appearance: textfield` as the default numeric input appearance.
